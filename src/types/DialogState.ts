// Defines DialogState interface with showDialog boolean property
// to track whether dialog should be shown

// Common DialogState Interface
export interface DialogState {
    showDialog: boolean;
}
